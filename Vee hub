-- // Configuration \\ -- local HubNameText = "Vee Hub" local AssetID = "rbxassetid://108339600962634"  local LoadstringURL = "https://pastebin.com/raw/XtPMhi9n"  -- // Services \\ -- local UserInputService = game:GetService("UserInputService") local CoreGui = game:GetService("CoreGui")  -- // UI Creation \\ -- local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "VeeHub_Final" ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling  -- Safety: Try to parent to CoreGui, fallback to PlayerGui if pcall(function() ScreenGui.Parent = CoreGui end) then     ScreenGui.Parent = CoreGui else     ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui") end  -- 1. Draggable Toggle Button (Vee Icon) local ToggleButton = Instance.new("ImageButton") ToggleButton.Name = "VeeToggle" ToggleButton.Parent = ScreenGui ToggleButton.Size = UDim2.new(0, 65, 0, 65) ToggleButton.Position = UDim2.new(0.05, 0, 0.4, 0) ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0) ToggleButton.BackgroundTransparency = 0.3 ToggleButton.Image = AssetID ToggleButton.ScaleType = Enum.ScaleType.Fit  local ButtonCorner = Instance.new("UICorner") ButtonCorner.CornerRadius = UDim.new(1, 0) ButtonCorner.Parent = ToggleButton  local ButtonStroke = Instance.new("UIStroke") ButtonStroke.Parent = ToggleButton ButtonStroke.Thickness = 3  -- 2. The Main Menu local MainFrame = Instance.new("Frame") MainFrame.Name = "MainFrame" MainFrame.Parent = ScreenGui MainFrame.Size = UDim2.new( -- // Configuration \\ -- local HubNameText = "Vee Hub" local AssetID = "rbxassetid://108339600962634"  local LoadstringURL = "https://pastebin.com/raw/XtPMhi9n"  -- // Services \\ -- local UserInputService = game:GetService("UserInputService") local CoreGui = game:GetService("CoreGui")  -- // UI Creation \\ -- local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "VeeHub_Final" ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling  -- Safety: Try to parent to CoreGui, fallback to PlayerGui if pcall(function() ScreenGui.Parent = CoreGui end) then     ScreenGui.Parent = CoreGui else     ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui") end  -- 1. Draggable Toggle Button (Vee Icon) local ToggleButton = Instance.new("ImageButton") ToggleButton.Name = "VeeToggle" ToggleButton.Parent = ScreenGui ToggleButton.Size = UDim2.new(0, 65, 0, 65) ToggleButton.Position = UDim2.new(0.05, 0, 0.4, 0) ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0) ToggleButton.BackgroundTransparency = 0.3 ToggleButton.Image = AssetID ToggleButton.ScaleType = Enum.ScaleType.Fit  local ButtonCorner = Instance.new("UICorner") ButtonCorner.CornerRadius = UDim.new(1, 0) ButtonCorner.Parent = ToggleButton  local ButtonStroke = Instance.new("UIStroke") ButtonStroke.Parent = ToggleButton ButtonStroke.Thickness = 3  -- 2. The Main Menu local MainFrame = Instance.new("Frame") MainFrame.Name = "MainFrame" MainFrame.Parent = ScreenGui MainFrame.Size = UDim2.new(
